import Component from "$store/components/search/Searchbar.tsx";
import type { Props } from "$store/components/search/Searchbar.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
